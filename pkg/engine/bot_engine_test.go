package engine

import (
	"testing"
)

func TestMultiply(t *testing.T) {

	// scalefactor := 10
	// expectedValue := 10

	// if got := Multiply(scalefactor); got != expectedValue {
	// 	t.Errorf("Multiply(%d) = %v (expectedValue: %d)", scalefactor, got, expectedValue)
	// }
}
